{
  "name": "fomo-relay-monitor",
  "version": "1.3.1",
  "description": "Chrome/Edge extension for displaying Relay purchase events from a configurable private monitor API.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node --check relay-core.js && node --check monitor-config.js && node --check content.js && node --check background.js && node --check popup.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
